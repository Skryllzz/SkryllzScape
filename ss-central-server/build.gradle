version = '1.0'
project.ext {
    type = "server"
}

description = ""

apply plugin: 'application'

dependencies {
    compile project(':ss-server')
}

application {
    mainClassName = 'io.ruin.central.Server'
}
